#!/usr/bin/python3
"""Below is a function that checks if type is the same as data type"""


def is_same_class(obj, a_class):
    """conditional rendering"""
    if type(obj) == a_class:
        return True
    return False
